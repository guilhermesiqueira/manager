// eslint-disable-next-line no-shadow
export enum Gateways {
  // eslint-disable-next-line no-unused-vars
  stripe = "Stripe",
  // eslint-disable-next-line no-unused-vars
  stripe_global = "Stripe Global",
}
